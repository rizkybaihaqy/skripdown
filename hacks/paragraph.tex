% Indent ALL (latex default dont indent first para) 10mm
\usepackage{indentfirst}
\setlength{\parindent}{10mm}

% Set Space 12pt after paragraph
% Becareful with this parskip here. i will add space to every element.
\usepackage{parskip}
\setlength{\parskip}{12pt}
